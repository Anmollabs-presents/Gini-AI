{
  "entities": {
    "Message": {
      "title": "Message",
      "description": "A single chat message in the conversation.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user ID of the owner of this message." },
        "role": { "type": "string", "enum": ["user", "ai"], "description": "Who sent the message." },
        "content": { "type": "string", "description": "The actual text content of the message." },
        "timestamp": { "type": "string", "format": "timestamp", "description": "When the message was created." }
      },
      "required": ["uid", "role", "content", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Messages belonging to a specific user."
    }
  }
}
